class TextDocument {
    public:
        TextDocument();
        
        void Insert(Graphic*);
        // TextDocument* text = new TextDocument;
        // text->Insert(new ImageProxy("anImageFileName"));
};
